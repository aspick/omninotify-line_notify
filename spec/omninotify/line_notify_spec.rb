require "spec_helper"

RSpec.describe Omninotify::LineNotify do
  it "has a version number" do
    expect(Omninotify::LineNotify::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
